server:
  port: 8084
spring:
  application:
    name: order-service
  profiles:
    active: dev
#  datasource:
#    url: jdbc:mysql://${hm.db.host}:13306/hm-order?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: root
#    password: ${hm.db.pw}
#    type: com.alibaba.druid.pool.DruidDataSource
#    druid:
#      # 连接池核心参数
#      initial-size: 5       # 初始化连接数
#      min-idle: 5           # 最小空闲连接
#      max-active: 20        # 最大连接数
#      max-wait: 60000       # 获取连接超时时间（ms）
#      # 连接检测与回收
#      time-between-eviction-runs-millis: 60000  # 检测空闲连接间隔
#      min-evictable-idle-time-millis: 300000    # 连接最小存活时间
#      validation-query: SELECT 1                # 连接有效性检测 SQL
#      test-while-idle: true                     # 空闲时检测连接
#mybatis-plus:
#  configuration:
#    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
#  global-config:
#    db-config:
#      update-strategy: not_null
#      id-type: auto
logging:
  level:
    com.hmall: debug
  pattern:
    dateformat: HH:mm:ss:SSS
  file:
    path: "logs/${spring.application.name}"
knife4j:
  enable: true
  openapi:
    title: 黑马商城接口文档
    description: "黑马商城接口文档"
    email: zhanghuyi@itcast.cn
    concat: 虎哥
    url: https://www.itcast.cn
    version: v1.0.0
    group:
      default:
        group-name: default
        api-rule: package
        api-rule-resources:
          - com.hmall.order.controller
# keytool -genkeypair -alias hmall -keyalg RSA -keypass hmall123 -keystore hmall.jks -storepass hmall123